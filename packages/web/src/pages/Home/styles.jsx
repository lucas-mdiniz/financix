import styled from 'styled-components';
import Card from '../../containers/Card';

const StyledReportChartWrapper = styled(Card)`
  background: red !important;
`;

export default StyledReportChartWrapper;
