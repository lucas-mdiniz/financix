import styled from 'styled-components';

const DataTitle = styled.span`
  font-weight: bold;
  font-size: 1.2rem;
  line-height: 1.2rem;
`;

const DataWrapper = styled.p`
  margin: 15px 0;
`;

export { DataTitle, DataWrapper };
